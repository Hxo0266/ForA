# ForA — Autonomous Academic Aid Assistant

> **版本**：1.2.0　**更新日期**：2026-09-17
> **根目录**：`D:\Courses`（课程工作区的统一父目录）
> **一句话**：ForA 是你的「自主学术助理」——一套把课程材料的**整理、提炼、归档、错题收录**串成一体的自动化学习资料处理流水线。

---

## 0. 名字由来

| 项 | 值 |
|:--|:--|
| **全名** | Autonomous Academic Aid Assistant |
| **正式简称** | **ForA** |
| **别名** | AAAA / Four A（取全名首字母 A·A·A·A） |

> 它把「课程资料 → 丢进 inbox → 说一句口令 → 自动分类 / 提炼 / 归档」这套流程，逐步长成了一个可复用、可扩展、带教训库与自动核对的完整系统。

---

## 1. 核心功能

ForA 目前包含 **7 大功能**：

| # | 功能 | 触发口令 | 说明 |
|:--|:--|:--|:--|
| 1 | **Inbox 自动分类** | 「处理一下 inbox」 | 把 `0_inbox/` 里的材料按类别自动归位（课件/大纲/作业/小测/期中/期末/错题/其他），只移动不删除、可回滚 |
| 2 | **课件知识点提炼** | 「提炼 PPT 知识点」 | 把课件提取成原文+图表，生成同名 `.md` 知识点笔记（Essence） |
| 3 | **课程纲要生成** | 「生成 Outline of XXX」 | 合并教学大纲与教材目录，补全根目录的 `Outline of XXX.md` |
| 4 | **新课程一键部署** | 「创建 XXX 文件夹」 | 把整套流程（含 `_System/`、税表、教训库、脚本）复制成一门新课程 |
| 5 | **错题收录**（1.0.0 新增） | 见 §4 | 把做错的题收集到 `8_Correction_Notebook/`，支持 inbox 触发与对话直接触发两种方式 |
| 6 | **两道闸门 + 教训库** | 「开工检查」「跑一遍课程流程」 | 动手前「开工检查」、干完活「收工核对」，所有踩过的坑写进 `_System/ERROR_LOG.md` 并尽量做成可自动复查的规则 |
| 7 | **作业答案生成**（1.1.0 新增） | 「处理一下 inbox」时自动 | 作业收进 `4_Assignment/` 时，自动读作业、逐题给出答案与解析，生成配套答案文档 |

---

## 2. 系统组成

### 2.1 课程工作区目录结构（每门课一套）

```
<课程根>/                         例：D:\Courses\CHM1001
├── 0_inbox/                     ← 唯一入口：待分类材料先丢这里
├── 1_Syllabus/                  ← 教学大纲 + 全部课程非教学内容
├── 2_Textbook&Lecture_PPT/      ← 课件 + 教材（已合并）
├── 3_Essence/                   ← 每份课件对应的同名知识点笔记（.md）+ figures/
├── 4_Assignment/                ← 课程作业
├── 5_Quiz/                      ← 小测 / 随堂演练 / 课堂练习
├── 6_Midterm Examination/       ← 期中试卷/答案
├── 7_Final Examination/         ← 期末试卷/答案
├── 8_Correction_Notebook/       ← 错题本（★ 1.0.0 新增）
├── 10_Others/                   ← 兜底
├── README.md                    ← 一页纸速查
├── Outline of <CODE>.md         ← 课程纲要
└── _System/                     ← 引擎目录（见 2.2）
```

### 2.2 引擎目录 `_System/`（每门课一套）

| 文件/目录 | 是什么 | 需要动吗 |
|:--|:--|:--|
| `taxonomy.json` | **分类税表 = 唯一事实源**：目录名映射 + 各类关键词 + 跳过规则 | 想调分类规则时改这里 |
| `ERROR_LOG.md` | **教训库 + 核对的唯一事实源**（`AIE-nnn` 编号） | 发现新问题必须往里加 |
| `WORKFLOW.md` | 完整流程规格（最详细的技术文档） | 想深挖时读 |
| `scripts/health_check.py` | 两道闸门：开工检查 / 收工核对 / 心跳 | 别动（除非加核对项） |
| `scripts/run_pipeline.py` | 一键编排 | 别动 |
| `prompts/` | 跨平台 prompt | 想把流程搬到别的 AI 平台时用 |
| `extracted/` | 课件提取产物（中间件，可删） | 别动 |
| `logs/` | 每次运行报告 + 可回滚移动清单 + 心跳 | 出问题先来这里看 |

### 2.3 三个 Skill（装在 `~/.workbuddy/skills/`，跨课程共享）

| Skill | 职责 | 关键脚本 |
|:--|:--|:--|
| `course-inbox-triage` | Inbox 分类器（含错题路由，1.0.0 更新） | `triage_inbox.py` |
| `essence-extraction` | 课件 → 知识点笔记 | `extract_slides.py` |
| `course-workspace-bootstrap` | 一键部署到新课程（1.0.0 已含错题本目录） | `bootstrap_course.py` |

### 2.4 定时自动化

每周六凌晨自动把新课件变成笔记（各课时刻错开 15 分钟，避免同一时刻唤起多个会话）：

| 课程 | 自动化名称 | 时刻 |
|:--|:--|:--|
| CHM1001 | `CHM1001 课件自动提炼 Essence` | 每周六 00:15 |

> ⚠️ **调度规则限制（AIE-012）**：`rrule` 的 `BYHOUR` **只能是单个整点**。多值写法
> `BYHOUR=12,21` 会直接创建失败；多值只对 `BYDAY` / `BYMONTHDAY` 生效。
> 想一天跑两次 → **建两条自动化**，别往一个 `BYHOUR` 里塞值。

> 🔴 **2026-09-17 重要更正**：本表原记录的 ID `4c0bc2b5-…` 与「首次运行 2026-09-14 21:45」
> 是**母版 AIE2040 的记录**，随文档复制而来。本机 `D:\Courses` **此前从未真正注册过**
> 这条自动化（`automations` 表为空）——而 `AIE-011` 的心跳检查一直报绿，因为**心跳文件
> 从没被写过，检查便无从报警**。现已按实际登记，真实 ID `fd62caf2-fccc-4afb-9bd5-5b57351534bf`。
> 这正是 AIE-011 想防的「配置过一次 ≠ 一直在跑」，只是这次连「配置过」都没发生。

> 自动化**只提炼课件、绝不移动或删除 `0_inbox/` 里的文件**；没事时只回一行「本轮无待提炼课件」就退出。

---

## 3. 调用方式（对话提示词）

你不用记脚本命令，直接对 WorkBuddy 说话即可：

| 口令 | 会发生什么 | 底层动作 |
|:--|:--|:--|
| **「处理一下 inbox」** / 「处理 inbox」 | 开工检查 → 扫 `0_inbox/` 出分类计划 → 你点头后才移动 → 收工核对 | `triage_inbox.py`（默认 dry-run） |
| **「提炼 PPT 知识点」** | 找缺同名笔记的课件 → 提取原文与图表 → 写 `3_Essence/<同名>.md` → 收工核对 | `extract_slides.py` + 模型写笔记 |
| **「生成 Outline of CHM1001」** | 合并大纲与教材目录，补全 `Outline of CHM1001.md` | 模型读 `1_Syllabus/` + `2_Textbook&Lecture_PPT/` |
| **「创建 XXX 文件夹」** | 整套流程复制成一门新课程 | `bootstrap_course.py` |
| **「开工检查」** | 盘点上次没干完的活 + 异常 | `health_check.py --phase open` |
| **「跑一遍课程流程」** | 把检查→分类→提取→覆盖率→核对一次跑完 | `run_pipeline.py` |

**附带的习惯**：

1. **收尾一问**：「处理一下 inbox」跑完，助手会主动问「是否直接提炼 PPT 知识点？」并附上当前待提炼份数。**它只问不做**，等你点头。
2. **只问不做 vs 自动做**：分类永远不会自动移动文件；提取与写笔记在你开口或定时自动化触发时才做。
3. **作业答案自动生成**（1.1.0 新增）：「处理一下 inbox」把作业收进 `4_Assignment/` 时，助手会**自动**读这份作业、逐题给出答案与简要解析，生成配套文档 `<作业名>-答案.md` 放在同一目录。

---

## 4. 错题本（8_Correction_Notebook）★ 1.0.0 新增

每个课程子文件夹下都有一个 `8_Correction_Notebook/`，专门收集做错的题。收录有 **两种触发方式**：

### 4.1 两种收录触发方式

**方式 1 · Inbox 触发**

1. 你把**一道单独的题**（文件，如 `.md` / `.pdf` / 图片）放进该课程的 `0_inbox/`；
2. 说一句「处理一下 inbox」；
3. 分类器/助手会把它归入 `8_Correction_Notebook/`，而不是 `4_Assignment/`：
   - 文件名带错题关键词（`错题` / `错题本` / `易错` / `易错题` / `correction` / `wrong question` …）→ **自动**归入；
   - 文件名没有明显关键词、但内容确实是「一道单独的题」→ 由助手在分类复核时**判断**归入错题本。

**方式 2 · 对话直接触发**

你直接在对话里问一道题（不用放进 inbox），助手会：

1. 依据各课程子文件夹内的内容**自动识别**该题属于哪门课——日后每门课的 `Outline of XXX.md` 写好并完善后，**优先看这个文件**；
2. 识别后**直接**把它写成一条错题笔记（题目 + 解答 + 出处），放进对应课程的 `8_Correction_Notebook/`。

### 4.2 收录格式约定

- 一条错题一个 `.md` 文件，文件名以「日期 + 主题」开头，例如 `2026-09-15-电子构型-易错题.md`。
- 每条错题笔记建议包含：**题目**、**错误原因**、**正确解法**、**出处/关联知识点**（可回溯，防臆造）。

---

## 5. 维护规则（母版 AIE2040 自动跟进）

**母版 = `AIE2040`**：`_System/` 的最新版永远在 AIE2040；其他课程（CHM1001、MAT3007 等）是它的**快照复制**，本身**不会自动跟进**——母版每加一条教训，副本就落后一版，而落后的副本只会显示「全绿」、不会报错。

因此 ForA 定下一条**铁律**：

> **框架改动必须自动跟进所有课程。** 凡是对 `_System/` 框架层的改动——版本更新、目录增删、`taxonomy.json` / `health_check.py` / `run_pipeline.py` / `ERROR_LOG.md` / skill 脚本——都要**同步到每一门课程**（AIE2040、CHM1001、MAT3007……），逐课比对 `LESSON_CHECKS` 键集与 `ERROR_LOG` 的 ID 集，缺失项按「**脚本 + 条目 + 变更记录**」三件套回补（只补文档等于没补）。
>
> **唯一例外**：当用户**明确说**「只改某一门课（的文件夹）」时，才针对那一门单独修改；否则一律全量同步。

两个注意点：

1. **编号偏移**：MAT3007 因多一条独立教训（占用 AIE-020），**自 AIE-021 起整体 +1**。同步时要按该课编号空间换算，别把母版编号直接塞进去。
2. **回补不是「只补文档」**：脚本实现、ERROR_LOG 条目、变更记录三件套缺一不可；只补文档等于没补。

---

## 6. 更新日志

### 1.2.0 — 2026-09-17

**框架全面体检**，修复 3 个真实缺陷、把 6 条教训从「需人工」升级为自动核对。

- **修复：本机自动化从未真正注册（严重）** — `automations` 表为空。README / WORKFLOW 里记的 ID
  `4c0bc2b5-…` 是**母版 AIE2040 的记录**，随文档一起复制了过来。心跳检查一直报绿是因为
  **心跳文件从没被写过**。现按实际登记：真实 ID `fd62caf2-fccc-4afb-9bd5-5b57351534bf`。
- **新增 AIE-028（「无法自动核对」被滥用成挡箭牌）** — 复核发现 6 条挂「需人工」的教训里
  **有 5 条完全可以自动化**，补上后立刻抓出 3 个真实缺陷。`LESSON_CHECKS` 由 **21 条增至 28 条**，
  `👤 需人工` 清零（仅 AIE-028 自身保留，因其属元问题）。
  - `AIE-010` → 静态扫「裸词 `in` 判断结构」+ 结构解析器故障注入
  - `AIE-012` → 多值 `BYHOUR` 探测 + 文档守卫
  - `AIE-013` → 交接点在脚本与 skill 两层的落地检查
  - `AIE-015` → ERROR_LOG 继承说明 + 编号冲突检测
  - `AIE-016` → 跨课码字面量扫描
  - `AIE-025` → 空目录检测
- **修复：AIE-012 的虚假声明（教训库撒谎）** — 正文称「限制已写入 `WORKFLOW.md §8.1`」，
  实际 `WORKFLOW.md` 里 **`BYHOUR` 三个字一次都没出现过**。现已真正写入（含正误写法对照）。
  **教训库也是代码，它的陈述必须可被机器验证。**
- **修复：7 个内容目录全空（打包必丢，AIE-025）** — `0_inbox` / `1_Syllabus` / `4_Assignment` /
  `5_Quiz` / `6_Midterm Examination` / `7_Final Examination` / `10_Others` 现全部补上占位文件，
  内容不只是占位，还写清了该目录装什么、什么会进来。
- **消除假警报** — 引入 `is_placeholder()` / `content_files()`：占位文件不是材料，所有
  「目录里有没有东西」的检查统一排除它。否则 `0_inbox/README.md` 会被 AIE-002 永远报成
  「Inbox 残留」，用户天天看假警报，慢慢就无视这条检查了（**假警报比没警报更糟**）。
- **拉齐两份 skill 副本** — `~/.workbuddy/skills/`（真正加载的）与 `D:\Courses\skills\`（交接备份）
  已分叉 4 个文件，方向全是「home 新、courses 旧」。已用 home 版覆盖，复验完全一致。
  旧版备份在 `D:\Courses\skills\_backup-20260917-before-sync\`。

**新增的探针工程经验**（写进 AIE-028）：
> 写检查函数时连踩两次坑——先是没排除注释、docstring、自己体内的故障注入样本，把 5 处噪声报成缺陷；
> 改完又矫枉过正，把跳过逻辑写进扫描函数内部，导致探针**把自己的测试用例也跳过了**，
> 第一条注入测试就报「期望报出，实际没报」。**探针骗过了自己。**

### 1.1.0 — 2026-09-15

- 新增**母版自动跟进铁律**：框架改动默认同步所有课程，仅当用户明确指定单课时才单独修改（见 §5）。
- 错题本**图文核对接入 `health_check.py`**：新增 `AIE-026` 教训与核对项，复用 `split_img_dest`/`IMG_RE` 校验 `8_Correction_Notebook/*.md` 的图片链接（语法合法、相对路径、指向真实文件），并已做故障注入测试。
- 修复**空 Inbox 跑 `--apply` 谎报「已实际移动」**（新增 `AIE-027`）：`triage_inbox.py` 抽出 `apply_mode()`，让「模式」标签忠实反映真实动作而非命令行参数——空跑不再给指向不存在清单的 `--undo` 指令，收工核对不再误报 `FAIL AIE-005`。
- 新增**作业答案生成**：作业收进 `4_Assignment/` 时，助手自动读作业、逐题给出答案与解析，生成配套文档 `<作业名>-答案.md` 放在同一目录。

### 1.0.0 — 2026-09-15（初始版本）

**继承**（来自 CHM1001 交接流程）：
- Inbox 自动分类（9 类）、课件知识点提炼、大纲生成、新课程一键部署
- 两道闸门（开工检查 / 收工核对）+ 教训库 `ERROR_LOG.md`（25 条 `AIE-nnn`）
- 定时自动化（每周六提炼 Essence）

**本次新增**：
- 新增内容目录 `8_Correction_Notebook/`（错题本），并接入分类税表与分类器（错题关键词 → 错题本，优先级高于作业/小测）
- 新增两种错题收录触发方式：① inbox 触发；② 对话直接触发（依据课程内容 / `Outline of XXX.md` 自动识别课程）
- 同步更新：`course-inbox-triage` skill、`course-workspace-bootstrap`（未来课程也带错题本目录）、`health_check.py`（O5 扫描覆盖错题本）
- 建立 ForA 系统命名与本文档

---

## 7. 未来规划

- [ ] 为每门课补全 `Outline of XXX.md`，让「对话直接收录错题」的课程自动识别更精准
- [ ] 错题本可考虑做**复习提醒**（错题定期回顾 / 间隔重复）
- [ ] 把「对话直接收录错题」沉淀为一个独立 Skill（当前由 ForA 文档 + 记忆约定承载）
- [ ] 交接 AIE2040、MAT3007 后，为它们各自建立工作区与错题本，并按 §5 铁律回补框架

---

*ForA 1.2.0 — Autonomous Academic Aid Assistant（AAAA / Four A）。*
