# CHM1001 课程自动化学习流程 · 交接文档

> **交接内容**：`CHM1001/` 文件夹（连同一整套自动化流程与 3 个 WorkBuddy Skill）
> **交接日期**：2026-09-15
> **交接包**：`CHM1001-交接包-20260915.zip`
> **读法建议**：第 0–3 节照着做就能跑起来；第 4–11 节是说明书；第 12–13 节出问题时查。

---

## 0. 一句话说清这是什么

一个**课程资料的自动整理与提炼流水线**：

```
课程材料 → 丢进 0_inbox/ → 说一句「处理一下 inbox」
        → 自动归类（课件/大纲/作业/小测/期中/期末/其他）
课件     → 丢进 2_Textbook&Lecture_PPT/ → 说一句「提炼 PPT 知识点」
        → 自动提取原文与图表 → 生成同名 Markdown 知识点笔记
```

设计上有两条硬原则：

1. **脚本做确定性的事**（移动文件、提取文本、渲染图、核对规则），**模型做需要判断的事**（这份材料算哪一类、这份课件该提炼成什么样）。
2. **两道闸门 + 一本教训库**：动手前"开工检查"、干完活"收工核对"，所有踩过的坑都写进 `_System/ERROR_LOG.md`，并且尽量做成**能自动重新检查**的规则。所以它每用一次都比上一次稳一点。

---

## 1. 前置条件

| # | 需要什么 | 必需性 | 说明 |
|:--|:--|:--|:--|
| 1 | **WorkBuddy 客户端** | 必需 | 提供 Skill 机制与定时自动化。对话口令依赖它理解自然语言 |
| 2 | **Python 3.11+** | 必需 | 跑 `_System/scripts/` 下的脚本 |
| 3 | Python 依赖包 | 必需 | `python-pptx`（读 pptx）、`pymupdf`（读 pdf / 渲染图）、`python-docx`、`pypinyin`、`pillow` |
| 4 | **LibreOffice** | 可选 | 只有"把 pptx 渲染成图片"才需要。没装时课件仍能提取文字，只是笔记里**不会有图** |

**装依赖（推荐单独建虚拟环境，别污染全局）**：

```bash
cd CHM1001
python -m venv .venv
.venv/Scripts/pip install python-pptx pymupdf python-docx pypinyin pillow
# 之后所有命令里的解释器都用：CHM1001/.venv/Scripts/python.exe
```

**检查 LibreOffice 在不在**（装了才有图）：

```bash
ls "/c/Program Files/LibreOffice/program/soffice.exe"
```

> ⚠️ 本文档里的示例路径 `C:/Users/Lenovo/...` 是**原机器**的位置，你这边**必须换成自己的实际路径**。脚本本身不含任何硬编码路径，全部靠 `--root` 参数指定。

---

## 2. 三步装好

### 第 1 步：放文件夹

把 `CHM1001/` 整个目录放到你想放的位置（比如 `D:\Courses\CHM1001`）。

### 第 2 步：装三个 Skill

压缩包里有一个 `skills/` 目录，里面有 3 个 Skill。把它们**整个复制**到 WorkBuddy 的用户级技能目录：

| 平台 | 目标位置 |
|:--|:--|
| Windows | `C:\Users\<你的用户名>\.workbuddy\skills\` |
| macOS / Linux | `~/.workbuddy/skills/` |

复制完应该是这样：

```
~/.workbuddy/skills/
├── course-inbox-triage/          ← Inbox 分类器
│   ├── SKILL.md
│   ├── scripts/triage_inbox.py
│   └── references/folder_taxonomy.md
├── essence-extraction/           ← 课件 → 笔记
│   ├── SKILL.md
│   ├── scripts/extract_slides.py
│   └── references/essence_format.md
└── course-workspace-bootstrap/   ← 一键部署到新课程
    ├── SKILL.md
    ├── scripts/bootstrap_course.py
    └── references/course_workspace_layout.md
```

装好后可以对 WorkBuddy 说「**处理一下 inbox**」试触发——如果它开始跑开工检查，说明 Skill 认到了。

### 第 3 步：自检（必做）

```bash
PY="<你的路径>/CHM1001/.venv/Scripts/python.exe"
"$PY" "CHM1001/_System/scripts/health_check.py" --root "CHM1001" --phase both
```

**预期输出**（说明一切正常）：

```
[✔] 开工检查：通过 6 / 关注 0 / 待办 0 / 严重 0
[✔] 收工核对：通过 25 / 关注 0 / 待办 0 / 严重 0
    [ERROR_LOG 自检] 已过 25 条 / 命中 0 条
```

看到这个，环境就算通了。**如果这一步不通过，先别往下走**——第 13 节有排查表。

---

## 3. 心理模型：整条流水线长什么样

```
                    ┌─────────────────────────────┐
   你下载的材料  →  │  0_inbox/                   │
                    └──────────┬──────────────────┘
                               │ 口令：「处理一下 inbox」
                               │ （先开工检查 → 出计划 → 你点头 → 才移动）
        ┌──────────┬──────────┼──────────┬──────────┬──────────┐
        ▼          ▼          ▼          ▼          ▼          ▼
   1_Syllabus  2_Textbook   3_Essence  4_Assignment 5_Quiz   6/7_Midterm
   教学大纲    &Lecture_PPT  （笔记）   作业        小测       /Final
              课件+教材
                    │
                    │ 口令：「提炼 PPT 知识点」
                    ▼
        ┌───────────────────────┐
        │ _System/extracted/    │  提取出的纯文本 + [FIGURE] 标记
        │        ↓              │  ← 笔记的**唯一事实来源**
        │ 3_Essence/<同名>.md   │  图文混排笔记 + 3_Essence/figures/<slug>/
        └───────────────────────┘
                    │
                    ▼  口令：「跑一遍课程流程」= 把上面串起来 + 收工核对
```

**关键点**：笔记**不允许**凭印象或常识写，只从 `_System/extracted/` 里的提取文本写。这是防"AI 编造课件内容"的核心机制。

---

## 4. 目录与文件逐个说明

### 4.1 内容目录（9 个）

| 目录 | 含义 | 谁会往里写 | 备注 |
|:--|:--|:--|:--|
| `0_inbox/` | **唯一入口**。所有未处理的材料先丢这里 | 你 | 处理完必须为空 |
| `1_Syllabus/` | 教学大纲 + **全部课程非教学内容**（评分标准、考核方式、课表、学术诚信、教师信息） | 分类器 | 不只是"大纲" |
| `2_Textbook&Lecture_PPT/` | **课件 + 教材合并**（原来分成两个目录，现合并） | 分类器 / 你 | 只有**课件**会被提炼成笔记，教材/习题解答会被自动识别并跳过 |
| `3_Essence/` | 每份课件对应的**同名知识点笔记**（`.md`）+ `figures/` 图片 | 提炼流程 | 只收 `.md`，且必须与课件同名（见第 11 节） |
| `4_Assignment/` | 课程作业：homework / assignment / lab / project / 习题 / 实验报告 | 分类器 | 新增目录 |
| `5_Quiz/` | 小测、随堂演练、课堂练习 | 分类器 | 新增目录；`quiz` 已从"考试"里拆出来 |
| `6_Midterm Examination/` | 期中试卷、答案 | 分类器 | |
| `7_Final Examination/` | 期末试卷、答案 | 分类器 | |
| `10_Others/` | 兜底，无法归类的 | 分类器 | 留了 `8_` / `9_` 空档给以后加类别 |

> **目录名可以随意改**（比如把 `0_inbox` 改成 `收件箱`）。改完**只需**同步 `_System/taxonomy.json` 里的 `folders` 映射，代码里没有任何硬编码目录名。这是刻意的设计（教训 AIE-018 / AIE-024）。

### 4.2 引擎目录 `_System/`（平时不用动）

| 文件 / 目录 | 是什么 | 需要你动吗 |
|:--|:--|:--|
| `taxonomy.json` | **分类税表 = 唯一事实源**。目录名映射 + 各类关键词 + 跳过规则 | 想调分类规则时改这里（改完立即生效） |
| `ERROR_LOG.md` | **教训库 + 核对的唯一事实源**。25 条 `AIE-nnn` | 发现新问题必须往里加（第 10 节） |
| `WORKFLOW.md` | 完整流程规格（最详细的技术文档） | 想深挖时读 |
| `scripts/health_check.py` | 两道闸门：开工检查 / 收工核对 / 心跳 | 别动（除非要加核对项） |
| `scripts/run_pipeline.py` | 一键编排：把检查→分类→提取→覆盖率→核对串起来 | 别动 |
| `prompts/` | 跨平台 prompt（`01_inbox_triage.md` / `02_essence_extraction.md`） | 想把流程搬到别的 AI 平台时用 |
| `extracted/` | 课件提取出的 `.slides.txt` / `.slides.json` / `.figures.json` | 中间产物，删了会重新生成 |
| `logs/` | 每次运行的报告 + **可回滚的移动清单** `moved_<时间戳>.json` + 自动化心跳 | 出问题先来这里看 |
| `_quarantine/` | 历史故障残留（原机器上一个跑错位置的 0 字节文件） | **可以直接删**，与流程无关 |

### 4.3 根目录其他文件

| 文件 | 说明 |
|:--|:--|
| `README.md` | 一页纸速查（比本文档短，日常看这个就够） |
| `Outline of CHM1001.md` | 课程纲要：合并 `1_Syllabus/` 与 `2_Textbook&Lecture_PPT/` 生成 |

---

## 5. 六条自然语言口令（最常用）

**你不用记脚本命令，直接对 WorkBuddy 说话就行。** 口令是本流程设计出来的触发词：

| 口令 | 会发生什么 | 底层动作 |
|:--|:--|:--|
| **「开工检查」** | 盘点有没有"上一次没干完的活"：inbox 残留、上轮遗留、待提炼课件、过期提取物、异常文件、Others 漏判 | `health_check.py --phase open` |
| **「处理一下 inbox」** | 开工检查 → 扫 `0_inbox/` 出**分类计划**给你确认 → 你点头后才移动 → 收工核对 | `triage_inbox.py`（默认 dry-run） |
| **「提炼 PPT 知识点」** | 找 `2_Textbook&Lecture_PPT/` 里缺同名笔记的**课件** → 提取原文与图表 → 写 `3_Essence/<同名>.md` → 更新索引 → 收工核对 | `extract_slides.py` + 模型写笔记 |
| **「跑一遍课程流程」** | 把上面几件事一次跑完（分类阶段仍是 dry-run，不自动移动） | `run_pipeline.py` |
| **「生成 Outline of CHM1001」** | 合并大纲与教材目录，补全根目录的 `Outline of CHM1001.md` | 模型读 `1_Syllabus/` + `2_Textbook&Lecture_PPT/` |
| **「创建 XXX 文件夹」** | 把整套工作流（含 `_System/`、税表、教训库、脚本、prompt）复制成一门新课程 | `course-workspace-bootstrap` |

### 附带的两个习惯

1. **收尾一问**：「处理一下 inbox」跑完，助手会**主动**在回复末尾问一句「是否直接提炼 PPT 知识点？」并附上当前待提炼份数——你回一句就行，不用自己记着。**它只问不做**（写笔记耗时且产出文件，要你点头）。这条只在对话里生效，定时自动化不会发问。
2. **只问不做 vs. 自动做**：分类**永远不会**自动移动你的文件；提取与写笔记在**你开口**或**定时自动化**触发时才会做。

---

## 6. 不想用对话？直接手跑脚本

四个脚本的完整参数：

### 6.1 `health_check.py` —— 两道闸门

```bash
"$PY" "_System/scripts/health_check.py" --root "." --phase open          # 只做开工检查
"$PY" "_System/scripts/health_check.py" --root "." --phase close         # 只做收工核对
"$PY" "_System/scripts/health_check.py" --root "." --phase both          # 两个都做
"$PY" "_System/scripts/health_check.py" --root "." --phase close --quiet # 只出一行回执
```

| 参数 | 作用 |
|:--|:--|
| `--root` | 课程根目录（含 `_System/taxonomy.json`）；省略则从当前目录向上自动探测 |
| `--phase {open,close,both}` | 检查阶段 |
| `--quiet` | 只输出一行汇总 |
| `--beat NAME` | 检查完顺手写一份自动化心跳（值为自动化名称） |
| `--beat-processed "A,B"` | 写进心跳的"本轮处理清单" |

### 6.2 `triage_inbox.py` —— 分类器

```bash
# 默认就是 dry-run：只出计划，不动任何文件
"$PY" "$HOME/.workbuddy/skills/course-inbox-triage/scripts/triage_inbox.py" --root "."

# 确认计划没问题后，才加 --apply 真正移动（会写可回滚清单）
"$PY" "$HOME/.workbuddy/skills/course-inbox-triage/scripts/triage_inbox.py" --root "." --apply

# 反悔了：按时间戳一键撤回
"$PY" "$HOME/.workbuddy/skills/course-inbox-triage/scripts/triage_inbox.py" --root "." --undo 20260914-190531
```

| 参数 | 作用 |
|:--|:--|
| `--apply` | **真正执行移动**（缺省只出计划） |
| `--batch-size N` | 单批最多处理 N 个文件（默认 10，超出自动顺延） |
| `--all` | 不限制单批数量 |
| `--include "*.pdf"` | 只处理匹配该 glob 的文件 |
| `--undo <时间戳>` | 撤回某次运行 |
| `--taxonomy <path>` | 显式指定税表 |

### 6.3 `extract_slides.py` —— 课件提取

```bash
# 批量提取课件（默认自动跳过教材等非课件文件）
"$PY" "$HOME/.workbuddy/skills/essence-extraction/scripts/extract_slides.py" --root "."

# 只提取某一份
"$PY" "$HOME/.workbuddy/skills/essence-extraction/scripts/extract_slides.py" --root "." --deck "2_Textbook&Lecture_PPT/L05.pptx"
```

| 参数 | 作用 |
|:--|:--|
| `--deck <path>` | 只处理指定课件（可多次） |
| `--force` | 已存在结果也重新提取 |
| `--figures {auto,all,none}` | 图表导出策略，默认 `auto`（只为含图表的页导出） |
| `--figures-dpi N` | 渲染 DPI，默认 150 |
| `--figures-dir <path>` | 图表输出位置；**必须在课程根内**，否则脚本直接拒绝（防"生成物跑出课程目录"） |
| `--ocr-hint` | 对无文本 PDF 额外标注"疑似扫描件" |
| `--include-non-decks` | 连教材/习题解答一起提取（默认跳过，一般不需要） |

**产物**：

```
_System/extracted/<课件名>.slides.txt      逐页文本，含 [FIGURE] <相对路径> 标记
_System/extracted/<课件名>.slides.json     结构化（每页标题/正文/表格/备注/图形数）
_System/extracted/<课件名>.figures.json    图表清单
3_Essence/figures/<slug>/slide-NN.png      含图表页的整页渲染图
_System/logs/extract_<时间戳>.md            提取报告（成功/跳过/无法解析）
```

### 6.4 `run_pipeline.py` —— 一键编排

```bash
"$PY" "_System/scripts/run_pipeline.py" --root "."                 # 全流程（分类 dry-run）
"$PY" "_System/scripts/run_pipeline.py" --root "." --apply          # 全流程 + 真的移动
"$PY" "_System/scripts/run_pipeline.py" --root "." --check-only     # 只做两道闸门，不碰文件
"$PY" "_System/scripts/run_pipeline.py" --root "." --essence-only   # 只看还差哪些课件没笔记
```

| 参数 | 作用 |
|:--|:--|
| `--apply` | 分类阶段真正执行移动 |
| `--skip-triage` | 跳过分类 |
| `--essence-only` | 只做覆盖率自检 |
| `--force-extract` | 强制重新提取课件 |
| `--skip-health` | 跳过开工检查与收工核对 |
| `--check-only` | 只做开工检查 + 收工核对 |

> ⚠️ **`--apply` 是唯一会改动你文件的参数**。不加它，所有操作都是只读的。

---

## 7. 定时自动化（可选，但强烈建议）

原机器上配了一条自动化，每周六凌晨自动把新课件变成笔记：

| 项 | 值（原机器） |
|:--|:--|
| 名称 | `CHM1001 课件自动提炼 Essence` |
| 频率 | **每周六 00:15** |
| 干什么 | 找缺笔记的课件 → 提取 → 写笔记 → 更新索引 → 收工核对（并写心跳） |
| 没事时 | 只回一行「本轮无待提炼课件」就退出，不刷屏 |
| **绝不会做** | **移动或删除 `0_inbox/` 里的任何文件** |

### 你要新建一条（不要沿用原机器的 ID）

通过 `automation_update` 工具创建，字段如下：

| 字段 | 值 |
|:--|:--|
| `name` | `<你的课程代号> 课件自动提炼 Essence` |
| `cwds` | 课程根目录的绝对路径 |
| `scheduleType` | `recurring` |
| `rrule` | `FREQ=WEEKLY;BYDAY=SA;BYHOUR=0;BYMINUTE=15`（示意：每周六 00:15） |
| `status` | `ACTIVE` |
| `prompt` | **照抄压缩包里那份 prompt**，把里面所有路径与课程代号换成你自己的 |

两个必须注意的点：

1. **时刻要错开**。如果你同时跑多门课，把它们的自动化时间各错开 15 分钟，避免同一时刻唤起多个模型会话。
2. **`BYHOUR` 不支持多值**。一条自动化只能设一个整点（比如想早上 8 点和晚上 8 点各跑一次，得建两条）。这是踩过的坑（教训 AIE-012）。

---

## 8. 分类判据：你的文件会被归到哪

优先级从高到低（命中即停）：

| 级别 | 条件 | 目标 |
|:--|:--|:--|
| 1 | `final / finals / 期末 / 期末考试 / 末考` | `7_Final Examination/` |
| 2 | `midterm / mid-term / 期中 / 期中考 / 中段考` | `6_Midterm Examination/` |
| 3 | 测验词（`quiz / pop quiz / 小测 / 随堂 / 随堂演练 / 课堂练习`）**且**无讲次编号、无课件词 | `5_Quiz/` |
| 4 | 作业词（`homework / assignment / hw / lab / report / project / 作业 / 实验报告 / 习题`）**且**无讲次编号、无课件词 | `4_Assignment/` |
| 5 | 只有泛考试词（`exam / test / 试卷 / 真题 / 考试`），没标期中期末 | `10_Others/` + **标记需人工确认** |
| 6 | 大纲/非教学类词（`syllabus / course outline / grading policy / 教学大纲 / 考核方式 / 学术诚信`…）**且**无讲次编号、无课件词 | `1_Syllabus/` |
| 7 | `essence / summary / 笔记 / 知识点 / 提纲` **且**扩展名是 `.md` | `3_Essence/` |
| 7b | 同上但扩展名不是 `.md` | `10_Others/` + 标记 |
| 8 | 课件词（`lecture / ppt / chapter / 课件 / 讲义`）或讲次编号（`L03` / `Ch2` / `Week5`）或 `.ppt/.pptx/.key/.odp` | `2_Textbook&Lecture_PPT/` |
| 9 | 教材词（`textbook / 教材 / 课本`） | `2_Textbook&Lecture_PPT/`（与课件同目录，靠"是不是课件"再区分） |
| 10 | 都不中 → 读正文首段找特征 | 对应分类 + **标记需复核** |
| 11 | 兜底 | `10_Others/` |

**匹配语义**（三种缺一不可，否则税表里的关键词会静默失效）：

- 英文：**整词**匹配（`Midterm` 不会因含 `mid` 误伤），同时支持**关键词紧贴数字**的写法（`Assignment2` / `HW3` / `Quiz1` / `Lab1`）
- 中文：**子串**匹配
- 多词短语（`course outline`、`grading policy`）：按归一化后的整串匹配

**几个容易踩的优先级细节**：

- `Lecture 5 Outline.pdf` → 进 **课件**（有 `lecture`），不是大纲。大纲词优先级低于课件词，是刻意设计的。
- `Lecture 6 homework.pptx` → 进 **课件**（有讲次编号），不是作业。
- `final_exam_solutions.pdf` → 进 **期末**（有 `final`），不会被"答案"抢走。
- 想改这些规则，直接编辑 `_System/taxonomy.json` 的 `keywords`，改完立即生效。

---

## 9. 两道闸门

这是流程的一部分，**不是可选项**。

### 9.1 开工检查（`--phase open`，O1–O6）

动手之前先盘点：

| 编号 | 名称 | 查什么 |
|:--|:--|:--|
| O1 | Inbox 残留检查 | `0_inbox/` 有没有没处理的文件（附**预估归类**） |
| O2 | 上一轮未收尾项 | 上次有没有没干完的活（移动失败 / 重复待决 / 顺延未消化） |
| O3 | 课件待提炼清单 | `2_Textbook&Lecture_PPT/` 里有课件但 `3_Essence/` 没有同名笔记 |
| O4 | 提取物时效 | 课件更新过、但提取物还是旧的 → 笔记会基于过期内容 |
| O5 | 异常文件扫描 | 0 字节文件、散落的 Office 锁文件（`~$xxx`） |
| O6 | 漏判/错位复查 | `10_Others/` 里有没有其实能明确归类的文件 |

### 9.2 收工核对（`--phase close`）

脚本读取 `_System/ERROR_LOG.md` 里每条教训，执行对应的自动核对，输出固定回执：

```
[ERROR_LOG 自检] 已过 24 条 / 命中 0 条
```

**状态含义**：

| 标记 | 含义 |
|:--|:--|
| `✅ PASS` | 通过 |
| `ℹ️ NOTICE` | 正常作业中的待办，不是故障 |
| `⚠️ WARN` | 需要关注（可接受但要看一眼） |
| `❌ FAIL` | 有实质问题，先修再报 |
| `— N/A` | 本轮不适用（如还没有笔记可核） |
| `👤` | 无法自动核对，需人工过目 |

**退出码**：全绿 `0`；有 WARN `3`；有 FAIL `4`。所以可以直接接进 CI 或判断脚本。

### 9.3 25 条核对项一览

| 编号 | 自动核对内容 |
|:--|:--|
| AIE-001 | `3_Essence/` 只收与课件同名的 `.md` |
| AIE-002 | `0_inbox/` 处理完必须为空（仅允许临时文件） |
| AIE-003 | 每份课件都有同名笔记 |
| AIE-004 | 提取物不得过期（课件更新过就要重提取） |
| AIE-005 | 每次 `--apply` 都留下可回滚的 `moved_*.json` |
| AIE-006 | 报告已落盘且为可读 UTF-8 文本 |
| AIE-007 | 脚本语法自检通过、不留 `__pycache__` |
| AIE-008 | 笔记含「出处索引」段（防臆造） |
| AIE-009 | 报告显示真实文件夹名而非内部键名 |
| AIE-010 | 核对逻辑不得用过松的"子串包含" 👤 需人工 |
| AIE-011 | 自动化必须真的在跑（心跳检测） |
| AIE-012 | 定时自动化的 `BYHOUR` 不支持多值 👤 需人工 |
| AIE-013 | 流程终点要有明确的下一步引导 👤 需人工 |
| AIE-014 | 税表里的多词短语必须真的能命中 |
| AIE-015 | 复制到新课程时教训库要加继承说明 👤 需人工 |
| AIE-016 | 复制时脚本里不得残留硬编码课程名 👤 需人工 |
| AIE-017 | 笔记与图表双向对齐（防断图 / 防漏用） |
| AIE-018 | 税表声明的目录必须都存在（防改名脱节） |
| AIE-019 | 首次写心跳那一轮不得误报"无心跳"（故障注入自测） |
| AIE-020 | 术语与重点区域必须带英文名（英文优先） |
| AIE-021 | 「公式在图内」正文下划线 ↔ 文末清单一一对应 |
| AIE-022 | 生成物路径无空格 / 图片链接语法合法且可解析 |
| AIE-023 | 生成物不得逃出课程目录（父级无 `figures/` 残留） |
| AIE-024 | 目录改名/合并后识别层与税表一致（含"教材≠课件"） |
| AIE-025 | 打包交接/备份时空目录不进压缩包 → 解压后结构缺失 👤 需人工 |

> 带 **👤** 的 6 条（010 / 012 / 013 / 015 / 016 / 025）是"无法自动判定"的规则，脚本会明确标出来提醒你人工过目——**它们不会被静默跳过**。

---

## 10. ERROR_LOG 教训库（这套流程的灵魂，务必理解）

`_System/ERROR_LOG.md` 是本工作区的**过往教训库**，也是核对逻辑的**唯一事实源**。

### 规则（强制）

1. **开工前**跑 `--phase open`，通读一遍，确认没有"上一次没干完的活"。
2. **收工后**跑 `--phase close`，脚本按 `AIE-nnn` 逐条自动核对。
3. **发现问题 → 先修复 → 再把问题与教训收录进 ERROR_LOG**。硬性要求，不是可选项。
4. 新增条目的格式：

```markdown
## AIE-025 — 标题一句话 [严重|中等|低]

**场景**：
**问题**：
**根因**：
**纠正**：
**教训**：
**核对项**：（能自动核对就写，并去 health_check.py 的 LESSON_CHECKS 登记同 ID 函数）
```

### 两条铁律

- **编号 = 文件内现有最大编号 + 1**。先 `grep "^## AIE-"` 确认，**不要覆盖或重排已有条目**。
- **"只会 PASS 的检查等于没有检查"**。新增的核对项必须做**故障注入测试**——故意造出错误，确认它真的会 FAIL。已有多条检查（如 AIE-019、AIE-023）自身就带这个自测。

### 为什么要这么较真

两个例子，说明"看不见的失败"有多贵：

- **AIE-022**：课件文件名里有个空格。Markdown 的图片链接在空格处被截断，导致 **31 张图全部断链、文档完全无法预览**。而当时负责查图的 AIE-017 报的是 **PASS**——因为它检查的是"文件在不在硬盘上"，而不是"Markdown 语法对不对"。**验证的指标和真实的问题错位了。**
- **AIE-023**：从上层目录运行提取脚本，图就被写到了课程文件夹**外面**。当时所有核对项都只看课程目录**内部**，**谁都没发现**。

教训的共同点：**失败往往是静默的**。所以检查要写在"失败会留下痕迹"的地方，而且要真的能报警。

---

## 11. Essence 笔记规范（如果你想自己写或改）

每条硬规则都对应一条教训，别绕过：

| # | 规则 | 为什么 |
|:--|:--|:--|
| 1 | 每份笔记必须有 `## 出处索引` 段，标到具体张号 | 防臆造，一切结论可回溯（AIE-008） |
| 2 | 每个术语 + 每个小节标题都要有**英文名**，中英并存时**英文在前** | 便于检索与跨语言引用（AIE-020） |
| 3 | 公式只存在于图里、提取文本没抓到 → 正文该处**只写** `<u>公式在图内</u>`；文末用 `## 公式在图内（提取文本丢失）` 逐条列张号，该节必须是**全文最后一节** | 有固定写法才能自动核对（AIE-021） |
| 4 | 图必须贴在**对应知识点所在小节的末尾**，禁止堆到文末、禁止只写"见图" | 图文脱节等于没图（AIE-017） |
| 5 | 文件名与图片目录一律走 `slug()`（空格/特殊字符 → `-`），**绝不允许出现空格** | 空格会让图全断链（AIE-022） |
| 6 | 内容只能来自 `_System/extracted/` 的提取文本；不确定的标 `[待核实]` | 防编造 |
| 7 | 一份课件一个文件，不合并；已有用户手写内容先问再改 | |

**配对规则**：`2_Textbook&Lecture_PPT/<课件名>.<ext>` ↔ `3_Essence/<slug(课件名)>.md`。`slug()` 由 `extract_slides.py` 提供，是**唯一实现**——笔记名和图表目录名必须用同一个。

---

## 12. 安全承诺

| 承诺 | 实现 |
|:--|:--|
| **默认只出计划，不动文件** | 分类器不加 `--apply` 就是只读 |
| **永不删除** | 只做移动；重复文件只标记、交你决定 |
| **可一键回滚** | 每次移动写 `_System/logs/moved_<时间戳>.json`，`--undo` 撤回 |
| **单批上限 10 个文件** | 避免一次性大动作，超出自动顺延 |
| **笔记不臆造** | 先提取成文本，笔记只从那份文本写 |
| **生成物不越界** | 输出路径若指向课程根之外，脚本直接拒绝执行 |
| **不静默失败** | 每条教训要么有自动核对，要么明确标 `👤 需人工` |

---

## 13. 常见问题排查

| 症状 | 原因与处理 |
|:--|:--|
| 脚本报「找不到课程根」 | 确认 `--root` 指向的是**含 `_System/taxonomy.json`** 的目录。旧的"同时含 `Inbox/` 和 `PPT/`"判据已废弃 |
| **笔记里的图全断了** | 路径里有空格。跑 `health_check.py --phase close`，看 AIE-022 是否 FAIL；修法是重命名笔记与图表目录为无空格形式 |
| 提取报告说「无法渲染图表」 | 没装 LibreOffice。装一个即可；或把 `.pptx` 先另存为 PDF 再放回原目录 |
| 提取不出文字的 PDF | 大概率是**扫描件**（纯图片）。加 `--ocr-hint` 会标注出来；需要 OCR 工具或改用可复制文本的源文件 |
| 收工核对报 `WARN AIE-011`（无心跳） | 自动化从没跑过，或停了超过 3 天。若你还没配自动化，属正常；配了就要查 |
| **`O3` 待提炼清单一直不为空** | ① 笔记名与课件名没对上（必须按 `slug()` 配对）；② 该文件其实是**教材/习题解答**，本就不该提炼（检查 `taxonomy.json` 的 `non_deck_keywords`） |
| 改了目录名之后全线报错 | 同步 `_System/taxonomy.json` 的 `folders`。核对项 AIE-018 会直接告诉你哪个目录对不上 |
| **交接包解压后，`0_inbox` 等空目录不见了** | 压缩工具默认**不保存空目录**。本包已显式补写目录条目，正常解压不会缺；若你用了别的工具重打包，请在每个空目录放一个占位文件（如 `.gitkeep`）再压缩（教训 AIE-025）。**自查方法：解压到临时目录跑一次 `--phase both`，AIE-018 会直接报出缺哪个目录** |
| 收工核对报 `FAIL AIE-023` | 课程目录外面出现了生成物（如父级的 `figures/`）。把产物搬回课程内，并检查你是在哪个目录下运行脚本 |
| 同一批改脚本，改了没生效 | 同一文件的多次编辑**必须逐条串行提交**，并行批量会静默丢失（AIE-007）。改完 `grep` 复核关键行 |
| `__pycache__` 被报出来 | 用 `PYTHONDONTWRITEBYTECODE=1` 跑脚本；注意 `python -m py_compile` 会**无视**这个环境变量，语法自检改用 `ast.parse` |

---

## 14. 想把这套流程用在另一门课

```bash
"$PY" "$HOME/.workbuddy/skills/course-workspace-bootstrap/scripts/bootstrap_course.py" \
      --source "<作为模板的课程根目录>" \
      --target "<新课程根目录>" \
      --code   "<新课程代号，如 MAT3007>"
```

它会：

- 建好整套内容目录 + `_System/`
- 复制流程级资产（脚本、税表、prompt、WORKFLOW、ERROR_LOG 带**继承说明**）并替换课程代号
- 重建课程级文件（`README.md`、`3_Essence/00_Index.md`、`Outline of <CODE>.md` 骨架）
- **绝不复制**课程私有数据（`logs/`、`extracted/`、课件、笔记）

之后按第 7 节给新课程建一条**独立**的定时自动化。

> 重要认知：`bootstrap` 是**一次性快照复制**，不是持续同步。以后模板课新增了教训，已部署的课程**不会自动跟上**，而且落后的副本**照样显示全绿、不会报错**。所以模板更新后要逐课对比 `health_check.py` 的 `LESSON_CHECKS` 键集合与 `ERROR_LOG.md` 的编号集合，缺失项按「**脚本实现 + ERROR_LOG 条目 + 变更记录**」三件套一起补。

---

## 15. 交接包内容与已排除项

### 包内结构

```
CHM1001-交接包-20260915.zip
├── HANDOVER.md                 ← 本文档的副本
├── CHM1001/                    ← 课程工作区（整套流程 + 内容 + 笔记 + 图）
└── skills/                     ← 3 个 Skill，复制到 ~/.workbuddy/skills/
    ├── course-inbox-triage/
    ├── essence-extraction/
    └── course-workspace-bootstrap/
```

### 已排除的内容（原机器上仍保留）

| 已排除 | 原因 |
|:--|:--|
| `CHM1001/.workbuddy/` | 原机器的**自动化运行记忆**，绑定在原机器那条自动化的 ID 上。你自己的自动化会有新的 ID，留着只会混淆 |
| `CHM1001/_System/_quarantine/` | 历史故障残留（一个 0 字节的垃圾文件），与流程无关，**留着也无害，可直接删** |
| `__pycache__/` | Python 字节码缓存，属构建产物 |

### ⚠️ 关于空目录（本包已处理）

`0_inbox/`、`1_Syllabus/`、`4_Assignment/`、`5_Quiz/`、`6_Midterm Examination/`、`7_Final Examination/`、`10_Others/` 在交付时是**空目录**，而压缩格式默认**不保存空目录**——如果直接压，解压后这些目录会凭空消失，第一次自检就会报 `❌ FAIL AIE-018`。

**本包已显式补写目录条目**，解压后目录结构完整。你自己后续重新打包时请留意这一点（教训 AIE-025）。

验证方法：解压到临时目录，跑一次 `health_check.py --phase both`，`AIE-018` 为 `✅ PASS` 即说明结构完整。

### 建议

1. 拿到后先做第 2 节第 3 步的**自检**，确认环境通。
2. 把 CHM1001 里**原有的课程笔记**（`3_Essence/CHM1001W1-Electronic-Structure.md` 与 `figures/`）保留也行、删掉重做也行——它是在原机器上按第 11 节规范生成的示例。
3. **注意课程材料的分享范围**：包里含课件原件（`.pptx`）与整页渲染图，如果这些来自授课教师，请只在课程内使用。

---

## 16. 术语表

| 词 | 含义 |
|:--|:--|
| **Essence** | 每份课件对应的知识点提炼笔记（`.md`） |
| **税表 / taxonomy** | `_System/taxonomy.json`，目录名与分类关键词的配置 |
| **slug** | 把文件名安全化的处理（空格、特殊字符 → `-`，中文保留） |
| **两道闸门** | 开工检查 + 收工核对 |
| **AIE-nnn** | `ERROR_LOG.md` 里的教训编号 |
| **心跳（heartbeat）** | `_System/logs/automation_heartbeat.json`，证明自动化确实在跑 |
| **O1–O6** | 开工检查的 6 个检查项 |
| **dry-run** | 只输出计划、不实际改动文件 |
| **`_System/extracted/`** | 课件提取出的纯文本，写笔记的**唯一事实来源** |
| **课件 vs 教材** | 二者同放一个目录；"课件"才会被提炼，"教材/习题解答"会被自动跳过 |

---

## 附录 A：25 条教训索引

| 编号 | 标题 | 严重度 |
|:--|:--|:--|
| AIE-001 | `3_Essence/` 混入非 `.md` 素材 | 中等 |
| AIE-002 | Inbox 残留无人发现 | 中等 |
| AIE-003 | 课件 ↔ 笔记覆盖率无人核对 | 低 |
| AIE-004 | 课件更新后提取物未刷新 | 中等 |
| AIE-005 | 同分钟二次运行覆盖移动清单，undo 失效 | **严重** |
| AIE-006 | 日志被本地编辑器改成二进制 | 中等 |
| AIE-007 | 同批次多次编辑静默丢失 | **严重** |
| AIE-008 | 笔记缺「出处索引」无法溯源 | 中等 |
| AIE-009 | 报告显示内部键名而非文件夹名 | 低 |
| AIE-010 | 核对逻辑用「子串包含」判断，过松 | 中等 |
| AIE-011 | 写笔记缺自动触发，全靠人喊口令 | 中等 |
| AIE-012 | 自动化 `rrule` 不支持多值 `BYHOUR` | 低 |
| AIE-013 | 流程终点缺少明确的下一步引导 | 低 |
| AIE-014 | 关键词匹配器只支持整词，多词短语静默失效 | 中等 |
| AIE-015 | 复制到新课程时 ERROR_LOG 不能原样照搬不加说明 | 低 |
| AIE-016 | 整套复制时脚本里的硬编码课程名被一起带走 | 低 |
| AIE-017 | 笔记漏图 / 把图堆在文末，图文脱节 | 中等 |
| AIE-018 | 目录改名后流程全线失效（目录名硬编码） | 中等 |
| AIE-019 | 收工核对在写心跳之前执行，首次运行必然误报 | 低 |
| AIE-020 | 术语与重点区域缺英文名 | 中等 |
| AIE-021 | 「公式在图内」的写法没有固定形式 | 中等 |
| AIE-022 | 路径/文件名带空格导致图全断链 | **严重** |
| AIE-023 | 生成物跑到课程目录之外，污染父级文件夹 | 中等 |
| AIE-024 | 目录改名/合并后识别层没跟着改 | 中等 |
| AIE-025 | 打包交接/备份时空目录不进压缩包，解压后结构缺失 | 中等 |

> 想看每条的具体「场景 / 问题 / 根因 / 纠正 / 教训」，直接读 `CHM1001/_System/ERROR_LOG.md`。**那份文件比本文档更权威**——本文档只是导读。

---

## 附录 B：关键数字速查

| 项 | 值 |
|:--|:--|
| 内容目录 | 9 个（`0_inbox` ~ `10_Others`） |
| 开工检查项 | 6 个（O1–O6） |
| 自动核对项 | 19 个 |
| 需人工核对项 | 6 个（010 / 012 / 013 / 015 / 016 / 025） |
| 教训总数 | 25 条 |
| 单批移动上限 | 10 个文件 |
| 心跳失效阈值 | 3 天未运行即告警 |
| 图渲染 DPI | 150 |
| 交付时的核查结果 | 开工 6/0/0/0，收工 25/0/0/0，命中 0 条 |

---

**有搞不明白的地方，最直接的办法**：对 WorkBuddy 说一句「**开工检查**」，它会扫描整个工作区并告诉你现在的状态；再问它任何具体问题，它读的就是 `_System/ERROR_LOG.md` 和 `WORKFLOW.md` 这两份原始文件。
